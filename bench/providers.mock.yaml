# Recording mock upstream on loopback (busbar reaches it via a shared network
# namespace, so 127.0.0.1 is the mock — inside busbar's plaintext-loopback carve-out).
mock: { protocol: openai, base_url: "http://127.0.0.1:9001", error_map: {} }
